/*
 * main.c
 *
 *  Created on: Jul 5, 2022
 *      Author: Mohamed
 */

#include <stdio.h>
#include <string.h>

void main()
{   //Ex1
  /*	float a[2][2];
	float b[2][2];
	float z[2][2];
	printf("Enter the elements of the first array\n");
	for(int i=0;i<2;i++)
	{ for(int j=0;j<2;j++)
	  {
		printf("Enter a:%d%d\n",i+1,j+1);
		fflush(stdout);fflush(stdin);
		scanf("%0.1f",&a[i][j]);
	  }
	}
	printf("Enter the elements of the second array\n");
	for(int i=0;i<2;i++)
	{ for(int j=0;j<2;j++)
	  {
		printf("Enter b:%d%d\n",i+1,j+1);
		fflush(stdout);fflush(stdin);
		scanf("%0.1f",&b[i][j]);
	  }
	}

	  for(int i=0;i<2;i++)
		{ for(int j=0;j<2;j++)
		  {
			z[i][j]=a[i][j]+b[i][j];
		  }
		}
	  printf("sum of matrix:\n");
	      for(int i=0;i<2;i++)
	  		{ for(int j=0;j<2;j++)
	  		  {
	  			printf("%10.1f  ",z[i][j]);
	  		  }
	  		   printf("\n");
	  		}*/

	//Ex2(average of array)
   /*     int x;
	    printf("enter num of elements ");
		fflush(stdout);
		scanf("%d",&x);

	  float a[x];
	  for(int i=0;i<x;i++)
	  {
		  printf("enter number:");
		  fflush(stdout);fflush(stdin);
		  scanf("%f",&a[i]);

	  }

	 float sum=0;
	 float avr;
	 for(int i=0;i<x;i++)
	 {
		 sum+=a[i];
	 }
      avr=sum/x;
	 printf("avrege = %f",avr);*/


	//Ex3(transpose of an array)

  /*	int r,c;
	printf("enter number of colms and rows:");
	fflush(stdout);fflush(stdin);
	scanf("%d %d",&r,&c);
	int x[r][c];
	int t[c][r];

	for(int i=0;i<r;i++)
	{ for(int j=0;j<c;j++)
	   {
		printf("enter element %d%d:",i+1,j+1);
		fflush(stdout);fflush(stdin);
		scanf("%d",&x[i][j]);
	   }

	}


	for(int i=0;i<r;i++)
	{ for(int j=0;j<c;j++)
	  {
		t[j][i]=x[i][j];
	  }

	}

	for(int i=0;i<c;i++)
	{ for(int j=0;j<r;j++)
	  {
		printf("%d   ",t[i][j]);
	  }
      printf("\n");
	}*/


	//Ex4(insert elements in array)
	/*	int n;
		printf("Enter number of elements ");
		fflush(stdout);fflush(stdin);
		scanf("%d",&n);
		int x[n];
		for(int i=0;i<n;i++)
		{
			scanf("%d",&x[i]);

		}
		for(int i=0;i<n;i++)
		{

			printf("%d  ",x[i]);
		}

		printf("\n");
		int y;
		printf("enter the element to be inserted");
		fflush(stdout);
		scanf("%d",&y);
		int l;
		printf("enter the location");
		fflush(stdout);
		scanf("%d",&l);
		x[l+1]=y;
		for(int i=0;i<n;i++)
		{
			printf("%d  ",x[i]);
		}*/


	//Ex5(search for element in array)
	int n;
	printf("enter num of elements:");
	fflush(stdout);
	scanf("%d",&n);
	int a[n];
	for(int i=0;i<n;i++)
	{
	  scanf("%d",&a[i]);
	}

	for(int i=0;i<n;i++)
	{
		 printf("%d  ",a[i]);
	}
	printf("\n");
	int z;
	printf("enter the element to be searched :");
	fflush(stdout);
	scanf("%d",&z);

	for(int i=0;i<n;i++)
	{
		if(a[i]==z)
		{
			printf("number founded at %d",i+1);
			break;
		}
	}




}
